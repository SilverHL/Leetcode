//
// Created by neojxwang(Íõ¾¸ìÓ) on 2019-09-18.
//

#include <vector>

using std::vector;

vector<int> fairCandySwap(vector<int>& A, vector<int>& B)
{
    int szA = 0;
    int szB = 0;
    int mpA[10005];
    int mpB[10005];
    memset(mpA, 0, sizeof(mpA));
    memset(mpB, 0, sizeof(mpB));
    int nA = A.size();
    int nB = B.size();

    for (int i = 0; i < nA; ++i)
        

}

